# Copy this file to .env and adjust if needed (defaults work for a local
# Ollama install with default settings)
OLLAMA_HOST=http://localhost:11434
OLLAMA_MODEL=llama3.1

# Optional overrides
DATA_DIR=data
